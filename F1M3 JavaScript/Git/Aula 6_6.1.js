// Sem converter tipos
console.log("Sem converter tipos")

const prompt = require('prompt-sync')();

x = prompt("Digite o número 1: ")
y = prompt("Digite o número 2: ")

// Somando as duas variáveius strings
console.log("Somando as duas strings")
soma = x + y

console.log("\nx = ", x, "\ny =", y)
console.log("\nsoma = ", soma)

// ---------------------------------------

// Convetendo os tipos
console.log("Convertendo os tipos")

const prompt = require('prompt-sync')();

x = parseInt("Digite o número 1: ")
y = parseInt("Digite o número 2: ")

//Somando as duas variáveius strings
console.log("Somando as duas strings")
soma = x + y

console.log("\nx = ", x, "\ny =", y)
console.log("\nsoma = ", soma)