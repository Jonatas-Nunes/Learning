const prompt = require('prompt-sync')(); //importação do pacote prompt-sync

var nome = prompt('Digite o seu nome: ') // uso do pacote pormpt-sync

console.log(nome); //printou na tela o conteúdo digitado pelo usuário